// Розділ 2 «Проєктування програми»: підрозділи 2.1–2.6, діаграма класів
// (рисунок 2.1), фрагмент коду, посилання на джерела [1]–[6].
import { dirname, join } from "path";
import { fileURLToPath } from "url";
import { h2, body, codeBlock, figure, emptyLine } from "./helpers.mjs";

const __dirname = dirname(fileURLToPath(import.meta.url));

const folderTree = `oop-coursework/
├── apps/
│   ├── api/          серверна частина — ASP.NET Core Web API (C#)
│   │   ├── Models/        класи предметної галузі
│   │   ├── Storage/       сховище даних (JsonDataStore)
│   │   ├── Services/      керуюча логіка (DirectoryService)
│   │   ├── Contracts/     типи запитів і відповідей API
│   │   ├── Controllers/   HTTP-контролери
│   │   └── Data/          JSON-файли бази даних
│   ├── api.tests/    модульні тести (xUnit)
│   └── web/          клієнтська частина — React, TypeScript
└── reports/          пояснювальна записка`;

const minCompetitionFragment = `/// <summary>
/// Пошук мінімального конкурсу з даної спеціальності за обраною
/// формою навчання. Вузи, де форма не ведеться (null), пропускаються.
/// </summary>
public MinCompetitionResult? GetMinCompetition(string name, StudyForm form)
{
    var best = _store.Specialties
        .Where(s => string.Equals(s.Name, name.Trim(),
            StringComparison.OrdinalIgnoreCase))
        .Where(s => s.Competition.ByForm(form).HasValue)
        .OrderBy(s => s.Competition.ByForm(form)!.Value)
        .FirstOrDefault();
    return best is null
        ? null
        : new MinCompetitionResult(GetUniversity(best.UniversityId),
            best, form, best.Competition.ByForm(form)!.Value);
}`;

export const designContent = [
  // ── 2.1 ────────────────────────────────────────────────────────────
  h2("2.1 Структура проєкту та архітектурні рішення"),
  body(
    "Програму побудовано за клієнт-серверною архітектурою. Серверна частина — " +
    "це вебслужба на платформі ASP.NET Core Web API [1], написана мовою C# " +
    "[2]; саме в ній зосереджено всю об'єктно-орієнтовану логіку роботи: класи " +
    "предметної галузі, перевірку даних, запити та збереження бази у файли. " +
    "Клієнтська частина — односторінковий вебзастосунок, який працює у " +
    "браузері, звертається до сервера через HTTP-запити і відповідає лише за " +
    "відображення даних та взаємодію з користувачем. Після збирання " +
    "клієнтська частина роздається тим самим сервером як набір статичних " +
    "файлів, тому для запуску програми достатньо одного процесу.",
  ),
  body(
    "Виконання роботи у вигляді вебзастосунку замість настільного застосунку " +
    "Windows Forms погоджено з керівником роботи письмово (електронний лист " +
    "від 11.05.2026). Вимоги методичних вказівок при цьому збережено: вся " +
    "об'єктно-орієнтована частина виконана мовою C#, а клавіатурні конвенції " +
    "адаптовано до браузерного середовища, як описано у підрозділі 2.4.",
  ),
  body(
    "Проєкт організовано як монорепозиторій — єдиний репозиторій, що містить " +
    "серверну частину, клієнтську частину, модульні тести та пояснювальну " +
    "записку. Структура папок проєкту така:",
    { keepNext: true },
  ),
  ...codeBlock(folderTree),
  emptyLine(),

  // ── 2.2 ────────────────────────────────────────────────────────────
  h2("2.2 Схема організації даних"),
  body(
    "База довідника зберігається у двох файлах формату JSON [3]: " +
    "universities.json містить колекцію вузів, specialties.json — колекцію " +
    "спеціальностей. Серіалізацію та десеріалізацію виконує бібліотека " +
    "Newtonsoft.Json [4]; файли записуються з відступами, тому їх за потреби " +
    "можна переглянути у будь-якому текстовому редакторі.",
  ),
  body(
    "Зв'язок між сутностями організовано за принципом «один до багатьох»: " +
    "кожна спеціальність зберігає ідентифікатор свого вузу у полі " +
    "UniversityId. Завдяки зв'язку через незмінний ідентифікатор " +
    "перейменування вузу автоматично відображається в усіх переліках і " +
    "результатах запитів, а дані не дублюються.",
  ),
  body(
    "Запис файлів виконується атомарно: спочатку дані записуються у " +
    "тимчасовий файл, який потім переміщується на місце основного. Якщо збій " +
    "станеться під час запису, попередня версія файлу залишиться " +
    "неушкодженою. Під час запуску програма завантажує обидва файли; " +
    "відсутній файл означає порожню колекцію (перший запуск), а пошкоджений — " +
    "безпечний старт із порожньою колекцією та повідомлення користувачеві.",
  ),

  // ── 2.3 ────────────────────────────────────────────────────────────
  h2("2.3 Класи предметної галузі"),
  body(
    "Основні класи серверної частини та зв'язки між ними наведено на діаграмі " +
    "класів — рисунку 2.1. На діаграмі для кожного класу показано лише " +
    "найважливіші члени.",
  ),
  ...figure(join(__dirname, "..", "uml", "classes.png"), {
    width: 238, height: 880,
    number: "2.1", caption: "Діаграма класів",
  }),
  body(
    "Клас University подає вуз і містить ідентифікатор, найменування та " +
    "адресу. Клас Specialty подає спеціальність: ідентифікатор, посилання на " +
    "вуз UniversityId, код, назву, вартість контракту та конкурс минулого " +
    "року. Конкурс винесено в окремий клас Competition із трьома полями типу " +
    "decimal? — за денною, вечірньою та заочною формами навчання; значення " +
    "null означає, що форма не ведеться. Перелік StudyForm задає три форми " +
    "навчання, а метод ByForm повертає конкурс за вказаною формою.",
  ),
  body(
    "Competition спроєктовано як об'єкт-значення (value object): він не має " +
    "власного ідентифікатора, не існує окремо від спеціальності та зберігається " +
    "як її складова. Це відношення композиції — на діаграмі його позначено " +
    "зафарбованим ромбом. Таке рішення спрощує модель: конкурс не потрібно " +
    "окремо створювати, шукати чи видаляти, він завжди подорожує разом зі " +
    "своєю спеціальністю.",
  ),
  body(
    "Клас JsonDataStore інкапсулює роботу з файлами: тримає колекції вузів і " +
    "спеціальностей у пам'яті та надає операції завантаження і збереження. " +
    "Клас DirectoryService реалізує керуючу логіку довідника, описану в " +
    "підрозділі 2.5. Кожному класу та кожному відкритому члену передує " +
    "коментар українською мовою з описом призначення.",
  ),

  // ── 2.4 ────────────────────────────────────────────────────────────
  h2("2.4 Графічний інтерфейс"),
  body(
    "Інтерфейс складається з трьох сторінок: «Вузи», сторінки обраного вузу " +
    "та «Спеціальності». Угорі вікна розташовано постійну шапку з назвою " +
    "програми, вкладками навігації та кнопкою допомоги. Клієнтську частину " +
    "реалізовано мовою TypeScript із використанням бібліотеки React [5]: " +
    "кожна сторінка, кожна форма і кожне діалогове вікно — окремий компонент. " +
    "Зовнішній вигляд задано за допомогою бібліотеки стилів Tailwind CSS [6].",
  ),
  body(
    "Форми додавання й редагування та діалоги підтвердження виконано " +
    "модальними вікнами на основі компонента Dialog бібліотеки Radix UI, який " +
    "забезпечує закриття за клавішею Esc та коректне утримання фокусу " +
    "всередині вікна. Перевірка введених даних виконується і на клієнті (для " +
    "миттєвого відгуку), і на сервері (як остаточне джерело істини); " +
    "повідомлення про помилки виводяться біля відповідних полів без " +
    "термінів розробника.",
  ),
  body(
    "Програма дотримується клавіатурних конвенцій: Enter — підтвердження " +
    "активної форми; Esc — закриття модального вікна або скасування дії; Tab " +
    "і Shift+Tab — переміщення між полями у природному порядку; «?» — виклик " +
    "вікна допомоги з переліком клавіш. Методичні вказівки передбачають для " +
    "виклику довідки клавішу F1, однак у вебзастосунку натискання F1 " +
    "перехоплює браузер, який відкриває власну довідку; тому виклик допомоги " +
    "свідомо адаптовано до клавіші «?», а підказку про неї розміщено на " +
    "кнопці допомоги в шапці вікна.",
  ),

  // ── 2.5 ────────────────────────────────────────────────────────────
  h2("2.5 Керуюча логіка"),
  body(
    "Уся керуюча логіка зосереджена у класі DirectoryService. Він реалізує " +
    "операції додавання, редагування та видалення для обох сутностей, пошук " +
    "вузів за підрядком та три запити завдання: всі відомості щодо обраного " +
    "вузу, всі пропозиції за обраною спеціальністю з фільтром за вартістю та " +
    "пошук мінімального конкурсу. Запити реалізовано виразами LINQ над " +
    "колекціями у пам'яті.",
  ),
  body(
    "Перевірка введених даних побудована на словнику помилок: сервіс " +
    "перевіряє всі поля одразу і збирає повідомлення у словник «назва поля — " +
    "текст помилки», який повертається клієнту; клієнт виводить кожне " +
    "повідомлення біля свого поля. Цілісність даних підтримується каскадним " +
    "видаленням: вуз видаляється разом з усіма його спеціальностями, а " +
    "кількість спеціальностей, що буде втрачено, повідомляється користувачеві " +
    "ще в діалозі підтвердження.",
  ),
  body(
    "Нестандартним рішенням є пошук мінімального конкурсу з урахуванням " +
    "того, що в частині вузів обрана форма навчання не ведеться. Такі вузи " +
    "позначаються значенням null і пропускаються під час порівняння, тому " +
    "результат ніколи не спотворюється відсутніми даними:",
    { keepNext: true },
  ),
  ...codeBlock(minCompetitionFragment),
  emptyLine(),
  body(
    "Будь-який неочікуваний виняток перетворюється проміжним обробником на " +
    "відповідь із загальним ввічливим повідомленням без технічних подробиць, " +
    "тож користувач за жодних умов не бачить програмних винятків, а програма " +
    "продовжує працювати.",
  ),

  // ── 2.6 ────────────────────────────────────────────────────────────
  h2("2.6 Інструкція зі встановлення"),
  body(
    "Для встановлення програми потрібен комп'ютер під керуванням Windows, " +
    "macOS або Linux із встановленими: .NET SDK 10, Node.js версії 22 або " +
    "новішої та менеджером пакетів pnpm версії 9 або новішої.",
  ),
  body("Встановлення виконується так:", { keepNext: true }),
  body(
    "1) завантажити архів zip із репозиторію GitHub (посилання наведено в " +
    "додатку А) і розпакувати його у будь-яку папку;",
  ),
  body(
    "2) у корені розпакованої папки виконати команду pnpm install, а потім " +
    "pnpm build — буде зібрано клієнтську частину;",
  ),
  body("3) виконати команду dotnet run --project apps/api;"),
  body(
    "4) відкрити у браузері адресу http://localhost:5080 — з'явиться сторінка " +
    "«Вузи».",
  ),
  body(
    "Для видалення програми достатньо зупинити її (Ctrl+C у вікні термінала) " +
    "і видалити папку: всі дані зберігаються у підпапці Data всередині неї, " +
    "записи в реєстрі та системних папках не створюються.",
  ),
];
